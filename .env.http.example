# HTTP Server Configuration
# Copy this file to .env.http and customize for your environment

# Server Configuration
PORT=3000
HOST=0.0.0.0
NODE_ENV=development

# Required: Directory to index and analyze
MCP_SERVER_DIR=/path/to/your/codebase

# CORS Configuration
# Comma-separated list of allowed origins, or * for all
CORS_ORIGINS=*

# Database Configuration (optional, uses defaults if not set)
# MCP_DB_PATH=./data/code-graph.db

# Embedding Provider (optional)
# MCP_EMBEDDING_ENABLED=true
# MCP_EMBEDDING_PROVIDER=transformers
# MCP_EMBEDDING_FALLBACK=true

# Logging (optional)
# LOG_LEVEL=info
